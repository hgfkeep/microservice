server.port=8081

eureka.instance.hostname=local

# 是否注册自己，在多接点eurreka时使用
eureka.client.register-with-eureka=false

# 是否获取注册实例信息
eureka.client.fetch-registry=false

eureka.client.service-url.defaultZone=http://localhost:${server.port}/eureka/